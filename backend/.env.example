# 生产环境配置
DEBUG=False
HOST=0.0.0.0
PORT=8000

# CORS配置 - 生产环境请指定具体的前端域名
ALLOWED_HOSTS=["https://your-frontend-domain.vercel.app", "https://your-frontend-domain.netlify.app"]

# 文件上传配置
UPLOAD_DIR=/tmp/uploads
MAX_FILE_SIZE=52428800

# 数据库配置（如使用外部数据库）
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Redis配置（如使用外部Redis）
# REDIS_URL=redis://user:password@host:port/db